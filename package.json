{
  "name": "translate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chrome 划词翻译与中英对照插件",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build --config vite.content.config.ts && vite build && node scripts/patch-dist.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0",
    "@types/chrome": "^0.0.287",
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.7.3",
    "vite": "^5.4.11",
    "vue-tsc": "^2.2.0"
  }
}
